#include<stdio.h>
#include <locale.h>
int i;
char op;



main (void){
	setlocale(LC_ALL, "");
	
	printf("Voce deseja fazer o cadastro? (S) ou (N)")
	fflush(stdin)
	gets(i)
    if (i <= S)
    
 	
	
	while(op == 'S'){
		
		printf("Esolha o sorvete")
		("%i", )
		
		
		printf("Cadastrar0 novamente? (S)(N) \n");
		fflush(stdin);
		scanf("%c", &op);
		
		
		
		
	}
	
	system("pause");
	return 0;
}
